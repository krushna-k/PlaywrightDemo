import {test, expect} from '@playwright/test';

//for finding the how many frames are available in the page we need use page.frames()


 //if you want to locate the element you can use page.frame({name: "kk"})

 await page.goto("");